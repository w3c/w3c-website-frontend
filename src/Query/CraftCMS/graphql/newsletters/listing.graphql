query NewslettersListing($siteId: [QueryArgument]) {
    entry(section: "newsletterArchive", siteId: $siteId) {
        id
        slug
        title
        uri
        expiryDate
        breadcrumbs: parent {
            ...breadcrumbs
        }
        ... on newsletterArchive_newsletterArchive_Entry {
            excerpt
            lead: pageLead
            seo: seoOptions {
                ...seoData
            }
        }
    }
}
