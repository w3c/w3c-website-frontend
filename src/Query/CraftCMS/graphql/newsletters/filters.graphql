query NewsletterFilters($siteId: [QueryArgument]) {
    first: entry(section: "newsletter", siteId: $siteId, orderBy: "postDate asc") {
        year: postDate@formatDateTime(format: "Y")
    }
    last: entry(section: "newsletter", siteId: $siteId, orderBy: "postDate desc") {
        year: postDate@formatDateTime(format: "Y")
    }
}
