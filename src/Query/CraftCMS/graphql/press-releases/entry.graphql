query PressReleases($slug: [String], $siteId: [QueryArgument]) {
    entry(slug: $slug, siteId: $siteId, section: "pressReleases") {
        status
        uri
        title
        slug
        language
        localized {
            title
            language_code: language
            uri
        }
        postDate
        dateUpdated
        expiryDate
        ... on pressReleases_default_Entry {
            excerpt
            displayYouMayAlsoLikeListing
            groups: workingGroups {
                title
                slug
            }
            specifications {
                title
                slug
            }
            ecosystems {
                title
                slug
            }
            defaultFlexibleComponents(orderBy: "sortOrder") {
                ...defaultFlexibleComponents
            }
            seo: seoOptions {
                ...seoData
            }
            notes: postPageNotes
        }
    }
}
