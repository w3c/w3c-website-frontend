query GroupInfo($siteId: [QueryArgument], $type: [QueryArgument], $slug: [String]) {
    category(siteId: $siteId, group: "groups", slug: $slug, groupType: $type) {
        id
        title
        uri
        slug
        ... on groups_Category {
            type: groupType
        }
    }
}
