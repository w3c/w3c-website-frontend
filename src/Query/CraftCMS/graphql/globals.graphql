query Globals($site:[String]) {
  globalSet(site: $site) {
    ... on footerSocials_GlobalSet {
      socialMedia {
      	... on socialMediaItem_Entry {
          title
          urlLink
        }
      }
    }
  }
}